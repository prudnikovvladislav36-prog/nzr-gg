# NZR.GG v0.9

- Исправлены ссылки общей шапки в `/admin/`: теперь NEWS / ESPORTS / UPDATES / INDUSTRY ведут в разделы основного сайта.
- Добавлен `boot.js`: `content.js` загружается с уникальным cache-busting параметром при каждом открытии страницы, поэтому новые публикации не должны зависать в старом кэше GitHub Pages/браузера.
- Сохранена первая реальная NEWS-публикация `xbox-disc-to-digital` и её обложка.
- Удалена демонстрационная NEWS-запись `project-enters-production`.
- Материалы по-прежнему сортируются по дате, а при одинаковой дате новая запись, вставленная админкой первой, остаётся выше старых.
